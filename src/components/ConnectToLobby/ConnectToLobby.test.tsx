import React from 'react';
import { render } from '@testing-library/react';
import ConnectToLobby from './ConnectToLobby';

describe('', () => {
  test('should render modal window Connect to lobby', () => {
    // const { getByText } = render(<ConnectToLobby label="Connect To Lobby" />);
    // expect(getByText(/Connect To Lobby/i)).toBeInTheDocument;
  });
});
