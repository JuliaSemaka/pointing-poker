import React, { useState } from 'react';
import { Button } from '../components/UI/Button/Button';
import { EButtonStyle } from '../components/UI/ui.module';

export const Start: React.FC = () => {
  return <div>Start</div>;
};

export default Start;
